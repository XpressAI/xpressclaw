feat: completes memory hooks — all pieces now wired. (1) Harness compact endpoint: POST /v1/session/compact resets session_id in claude-sdk harness, no-op stub in base. (2) HarnessClient::compact() for server-side triggering. (3) Token tracking in processor.rs: estimates conversation size from message content lengths, when >100k chars triggers consolidation then compaction. This is rough but effective — avoids needing the SDK's internal token counting. (4) Idle consolidation in reconciler.rs: when an agent goes idle and has remember hooks, spawns async consolidation task. Uses tokio::spawn since reconcile_idle_tasks is sync. (5) Fixed hardcoded 'least-recently-relevant' eviction strings — now reads config.memory.eviction everywhere. (6) Passed config through call_agent and process_response signatures so they can access memory config.
