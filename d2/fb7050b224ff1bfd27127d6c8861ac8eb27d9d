Fixed BOTH wizard paths (complete_setup AND add_agent). The initial wizard had the same ..Default::default() bug that dropped skills and didn't merge default MCP servers. Also set HOME=/workspace in containers — agents were writing to /home/agent because that was the default HOME. Added test assertions to catch this: test_complete_setup now verifies skills=[memory-system, task-management, build-app] and mcp_servers contains tasks/memory/skills/apps/shell/filesystem.
