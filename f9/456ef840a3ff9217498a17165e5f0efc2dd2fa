fix: reverted the auto-create conversation from the previous commit — tasks from the task board should NOT create conversations (those show in the sidebar and pollute the conversation list). conversation_id is only for tasks that originate FROM a conversation. Also filtered subtasks from the main task list with parent_task_id IS NULL — subtasks are lightweight steps inside a parent task, not independent kanban items.
