Final two top-10 review findings (#9, #10). #9: toDevice in AndroidLiveView.svelte now clamps to [0, deviceW-1]/[0, deviceH-1] via a clamp() helper — setPointerCapture lets a drag release outside the letterboxed frame, which produced out-of-range device px. #10: screenshot_scaled guards the resize (only when img.width().max(height) > max_dim) because image::resize scales to fit bounds in BOTH directions and thus UPSCALES sub-max_dim frames (720x1280 AVD -> 882x1568). Verified: 9 core tests pass, workspace+frontend check clean. All 10 top findings from the review are now addressed across 3 commits (5443db0 injections+gate, f2e1fa0 targeting+probe+errors, this one). Still open (minor, not top-10): setTimeout(refresh,350) in post() not tracked in frameTimer (can race frame loop), mcp_android.py opens httpx.Client per call, ADR-024 lists 6 tools but MCP ships 10, unix emulator spawn has no setsid/process-group (SIGHUP risk).
