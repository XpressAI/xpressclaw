feat: Major local LLM fix bundle. (1) Ollama explicit wizard option with model list, auto-pull via reconciler. (2) Embedded llama.cpp CUDA fixes: unified memory, n_gpu_layers=MAX, auto-fit ctx, Q8_0 KV cache. (3) Streaming tool calls were broken — raw <tool_call> XML was sent as text instead of structured tool_calls. Fixed by buffering output when tools present and parsing both JSON and XML formats. Qwen3.5 prefers <function=name><parameter=key>value</parameter></function> XML format. (4) First-message SSE race condition fixed by awaiting EventSource open before sending pending message.
