Major architecture fix: eliminated dual source of truth. YAML is now the single source for agent config (role, model, tools, llm, budget, etc.). SQLite agents table only stores runtime state (status, container_id, timestamps). AgentRegistry.register() replaced by ensure() which does INSERT OR IGNORE. On startup, stale DB entries are pruned. API responses merge DB state + YAML config. RegisterAgent struct removed entirely.
