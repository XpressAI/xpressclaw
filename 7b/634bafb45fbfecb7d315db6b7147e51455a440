Senior wanted the android view beside the chat, not under workflows. Clarified 'rail' with fahreza: a rail is a thin edge strip (too thin for a phone preview), so model = rail(icon) + panel(spawns on click). 4th flex child in +layout (left sidebar->main->panel->rail). Rail only if androidAvailable (probe /v1/android/status; server route is #[cfg(feature=android)] gated so 404 when absent). androidProbed distinguishes server-not-up (retry) from feature-absent (stop) so non-android builds don't poll 404 forever. Panel open persists in localStorage. View extracted to lib/AndroidLiveView.svelte, reworked for the narrow panel. Two rounds of fahreza feedback: (1) phone was cut off / black side bars -> fit by height AND draw a device bezel (rounded neutral-900 frame hugging the screen, panel bg around it); (2) emoji nav -> SVG back(arrow)/home(house)/recents(square). /android route + workflows link removed. Verified in the installer by fahreza. Swipe is the SEPARATE next commit.
