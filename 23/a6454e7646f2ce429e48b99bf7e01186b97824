feat: memory hooks phase 1 — hippocampus sub-agent model. Design: memory runs server-side, harness-agnostic. Sub-agent calls go through harness send_task for synthesis. (1) Recall hook in processor.rs and dispatcher.rs: on first turn, checks if agent has memories (quick DB count), spawns sub-agent to search+synthesize narrative recollection, injects as conversation message (NOT system prompt — preserves caching). (2) Remember hook: async tokio::spawn after each response, sub-agent reviews exchange, saves fleeting notes to Inbox/ via save_memory MCP. Non-blocking. (3) Consolidate method ready but not yet wired to compaction trigger. The persistent session IS working memory — no need to recall every turn. Hooks are per-agent via existing HooksConfig (before_message: [memory_recall], after_message: [memory_remember]). Still needed: token tracking for compaction trigger, harness compact endpoint, end-of-session consolidation.
