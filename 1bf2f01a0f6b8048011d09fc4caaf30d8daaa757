feat: XCLAW-47 idle-tasks + XCLAW-48 UI fix. Idle tasks use desired-state reconciler pattern (not separate polling loop) — reconcile_idle_tasks() runs inside reconcile_once(). IDLE tasks are hidden (hidden=1, task_type='IDLE'), single-turn (auto-delete in dispatcher evaluate()), with platform-matching exponential backoff. idle_count on agents table, reset when normal tasks complete. Build.rs fixed: walk_dir emits rerun-if-changed per file (directories only detect top-level adds/removes), mtime comparison skips npm when output is fresh. Svelte  replaced with page.subscribe() to avoid  store triggering loadAgent() on every store update.
