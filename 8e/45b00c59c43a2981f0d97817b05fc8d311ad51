Follow-up to the rail (fahreza wanted rail and swipe as separate commits, and to VERIFY before committing - so this was built, tested, then committed). No backend work - /v1/android/swipe existed already. img onclick=handleClick -> onpointerdown/onpointerup: pressStart records start (device px)+Date.now(); on up, hypot(dx,dy)<SWIPE_THRESHOLD(12 device px) => tap (preserves old behavior), else swipe with ms=clamp(elapsed,50,800). setPointerCapture so pointerup fires if released off-image. CRITICAL FIX from fahreza's testing: first cut 'dragged the image' instead of swiping - the browser's native <img> drag. Fixed with draggable=false + e.preventDefault() in pointerdown + touch-none (touch) + select-none. Date.now() fine (browser, not Workflow sandbox). Verified working in the installer before commit.
