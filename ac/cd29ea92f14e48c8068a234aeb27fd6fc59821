The macOS launchd SSH-agent socket cannot be passed directly through the Podman VM filesystem; worker container creation fails before the harness starts. Podman upstream issue #23785 remains the reference. Docker Desktop has the host-services SSH socket bridge already handled by the runtime. The requested response is advisory, not enforcement: keep Podman configurable so future releases can be tested. A shared host-SSH control covers first setup, add-agent setup, and existing Agent settings. It uses the instance system-info and detected runtime, not the browser OS; only a new macOS/Podman opt-in opens the warning. Keep disabled and Escape preserve false, Enable anyway persists true, and existing true values remain unchanged. Detection errors log and show conditional guidance without preventing configuration. No backend or forwarding semantics were changed; ADR-036 documents the decision. Verified Svelte check (0 errors/warnings), frontend production build, repository-supported Rust formatting check, and 10 focused Chromium tests on both dev and production builds. Inspected desktop/light and mobile/dark dialog screenshots. Build emits circular-chunk warnings but the production browser checks pass. The legacy local pre-commit hook invokes prohibited cargo fmt --all; it was bypassed for this frontend-only commit after scripts/rustfmt.sh --check passed. User checkout edits were saved in a named stash before fast-forwarding main; the pre-existing external submodule checkout was left untouched.
