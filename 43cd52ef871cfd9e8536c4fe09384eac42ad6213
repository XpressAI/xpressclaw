Root cause: AppleScript document creation commands (save as, make new document) produce empty stdout. The agent received 'Script executed successfully.' with no evidence the file was created, so it assumed failure and retried indefinitely. Fix: after successful office_run, call the documents listing API and include file names + sizes in the response. Agent can now verify 'report.docx (15432 bytes)' exists.
