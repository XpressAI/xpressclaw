fahreza noticed the test plan used curl for swipe (no CLI equivalent). Audit: swipe/input_text/key_event existed in core + server routes + MCP shim but were never surfaced as CLI subcommands, and screen_elements (the compact screen map) was on server (/elements) + MCP (android_screen_map) but the CLI only had raw 'dump'. Added all four as CLI arms mirroring the existing tap/long-press ones (swipe/type->input_text/key->key_event/screen-map->screen_elements via serde_json). shell stays CLI-only by design (raw adb passthrough - not an agent/server surface); status stays server-only (doctor covers CLI health). Now every ACTION primitive is on all 4 layers; perception (dump + screen-map) also full parity. Live-tested all four on the emulator.
