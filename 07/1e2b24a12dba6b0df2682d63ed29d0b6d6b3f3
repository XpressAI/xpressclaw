Pre-test polish. Three things that would have made the user's first test fail silently: (1) wasm/shim paths were CWD-relative — now resolved against cwd + $XPRESSCLAW_REPO + exe dir + dev tree root with explicit not-found errors. (2) c2w-net was assumed in PATH — now also checks ~/.local/bin, ~/bin, ~/.cargo/bin. (3) Three Config literals in setup.rs used ..Default::default() which was silently flipping pi.enabled back to default whenever the user finished GGUF download or added an agent.
