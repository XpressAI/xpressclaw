Both found while fahreza tested the live view. REFRESH: measured frame latency ~1.1s (93KB JPEG) vs a 700ms setInterval - the img.src swap cancels each load before it finishes, so it never renders (only a fresh mount/navigate showed a frame). Load-gated loop (onload/onerror -> scheduleNextFrame after FRAME_GAP_MS=400) fixes it and self-restarts via <img> remount when reachable flips; document.hidden pause spares the device/laptop (fahreza's heat concern). TAP: /v1/android/screenshot downscales to 1568 long-edge = ~706x1568 for a 1080x2400 device; handleClick used imgEl.naturalWidth (=706) so taps compressed by 1080/706~=1.53x toward top-left ('touch doesn't work'). The click fraction is already resolution-independent - just needed device W/H, so added screen_size (wm size, Override-first) surfaced on /status. Verified live in the rebuilt installer: preview auto-refreshes ~1.5s and taps land where clicked. Ironic: this was the same image-scale bug the a11y design avoids, living in the human tap path.
