Browser automation via Playwright. Same pattern as office: agent writes script, xpressclaw executes on host, returns result. Three tools: browser_screenshot (saves to per-agent screenshots dir), browser_fetch (extracts text from JS-rendered pages — unlike HTTP fetch), browser_run (custom Playwright Python scripts with $SCREENSHOTS_DIR injection). Requires playwright installed on host (pip install playwright && playwright install chromium).
