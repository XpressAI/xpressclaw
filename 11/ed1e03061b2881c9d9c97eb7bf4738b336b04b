fix: stop button threw 'string did not match expected pattern' because the stop endpoint returns 200 with no body, and the generic request() helper always called res.json(). Fixed by checking content-length and reading as text first. Also bumped llama-cpp-rs submodule to latest llama.cpp master (b8635075) which has Gemma 4 tokenizer fix, final_logit_softcapping, custom newline split, and specialized parser.
