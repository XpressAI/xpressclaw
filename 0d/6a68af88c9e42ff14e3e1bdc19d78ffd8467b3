Addressed the PR narrow-screen review without expanding ADR-039 scope. The global MCP settings header now stacks its heading and action group below the sm breakpoint while preserving the desktop row layout. The discovery Playwright case now loads /settings/mcp at 390x844 before asserting both actions are visible and the document has no horizontal overflow, closing the prior coverage gap. Kept unrelated task/workspace work unstaged and outside this commit. Validation: focused discovery test passed; npm run check passed with zero diagnostics; npm run build passed; full Playwright suite passed 65/65 with one worker.
