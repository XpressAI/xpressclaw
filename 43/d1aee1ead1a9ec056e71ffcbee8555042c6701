Removed @modelcontextprotocol/server-fetch — it was hallucinated (404 on npm). Cleaned out from: researcher preset template (default_mcp_servers + default_tools), setup wizard UI (fetchEnabled state, checkbox, URL pattern config), and 3 tests that asserted on fetch MCP server. Tests now use websearch from the researcher preset instead. The Cargo.lock change is unrelated (came from a prior rebase).
