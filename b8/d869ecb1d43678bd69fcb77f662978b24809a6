App container error was because agent's /workspace is container-internal — no host path to mount. Fix: shared Docker named volume (xpressclaw-workspace-{agent}). Both agent and app containers mount the same volume. App container runs start_command with working_dir=/workspace/apps/{app_id}/. Also added cmd and working_dir to ContainerSpec — previously containers could only use image default CMD.
