{
  "identifier": "default",
  "description": "Default permissions for xpressclaw desktop app. No `remote` grant: these permissions (fs, shell, dialog, clipboard) are NOT exposed to the frontend served over http://localhost — only the minimal window-creation grant in remote-webview.json is.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    {
      "identifier": "shell:allow-spawn",
      "allow": [
        {
          "name": "binaries/xpressclaw",
          "sidecar": true,
          "args": true
        }
      ]
    },
    "clipboard-manager:allow-read-text",
    "clipboard-manager:allow-write-text",
    "clipboard-manager:allow-read-image",
    "clipboard-manager:allow-write-image",
    "dialog:default",
    "fs:default",
    "opener:default"
  ]
}
