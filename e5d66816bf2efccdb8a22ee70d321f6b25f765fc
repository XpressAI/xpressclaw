feat: lazy model loading via LazyLlamaCppProvider. Uses OnceLock to defer GGUF load until first chat(). Startup validates file path only (~0 RAM). Model loads on first request. Thread-safe via OnceLock — loaded exactly once. The 24GB Mac tester was OOMing because the 9B model (old RAM requirements) loaded at startup alongside Docker (~3GB) leaving no headroom. PR #83 already fixed the recommendation (now recommends 4B for 24GB), this PR fixes the memory spike at startup.
