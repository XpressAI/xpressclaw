Eduardo requested a task workflow where clicking a current workspace change opens the selected file beside the Task, without losing the Task details sidebar, and with Monaco receiving more space from an initially collapsed tree. ADR-034 remains immutable and authoritative for workspace access, Git status, file reads/diffs/saves, revision conflicts, terminals, and security; ADR-040 records only the new frontend composition and supersedes the implicit replace-current-view behavior. The implementation reuses the existing workspace pane manager and Agent Files APIs instead of adding another editor, split system, backend route, or global state. The selected path and initial tree state are encoded in the workspace tab route so restored tabs reconstruct deterministically. Normal clicks request a split only when measured workspace width leaves at least 480px per pane; mobile/constrained layouts and pane-limit cases fall back to same-pane Files navigation. Command/Ctrl, Shift, and Alt clicks keep native link behavior. Alternatives rejected: editing ADR-034, device/user-agent checks, always splitting into cramped panes, remembering a global tree preference, and adding backend attribution or editor state. Validation: synchronized release metadata, shell syntax, standalone installer tests, frontend Svelte/type check with zero diagnostics, production frontend build, focused workspace/editor cases, full Playwright suite 94/94, final four feature cases, diff check, and visual review of collapsed, reopened-tree, and constrained-fallback screenshots under docs/assets.
