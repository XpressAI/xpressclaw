Redesigned browser automation: Chrome launches on host with --remote-debugging-port=9222, Playwright inside the container connects via CDP (connect_over_cdp). This means no Playwright installation needed on the host — just Chrome. The agent's tools run inside the container, connecting to the host browser. Auto-detects Chrome/Chromium/Edge paths on all platforms. Browser is visible on screen so user can watch.
