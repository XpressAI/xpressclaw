Skills were listed by name in the system prompt with 'call read_skill to load instructions'. The agent never called read_skill — it had no reason to. Fix: inject the full SKILL.md body directly into the system prompt. The build-app skill now tells the agent to use create_app_workspace → publish_app, use /workspace/apps/, listen on PORT env var, etc. The read_skill MCP tool still exists for dynamic skill loading but the core skills are always present.
