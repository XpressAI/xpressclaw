feat: full task interaction model. Design principle: tasks are NOT conversations — they have their own UI, message stream (task_messages table), and interaction model. Key additions: (1) Dispatcher handles WaitingForInput — evaluate() detects it, returns Paused, poll_once marks queue complete. load_task() accepts WaitingForInput for resumption. (2) MCP tool request_input in mcp_tasks.py — saves question as assistant message, sets status to waiting_for_input. (3) add_message endpoint in tasks.rs — when role=user and task is waiting_for_input, transitions to in_progress and re-enqueues. (4) Frontend message input matching conversation page style (textarea + send button, Enter to send). (5) Inline subtask checklist with checkbox indicators instead of page links.
