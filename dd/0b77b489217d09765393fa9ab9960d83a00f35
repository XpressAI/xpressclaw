Phase 5: managed emulator lifecycle, built to mirror the existing Docker pattern exactly (user's explicit ask: 'like how we check if docker is installed/up, and if not spawn it'). Docker reference: DockerManager::connect (running) / is_docker_desktop_installed / start_docker_desktop, surfaced via setup routes check_docker {available,installed,can_start} + start_docker, with a frontend auto-modal.

Android mirror: android::emulator module — is_running(serial) probes adb_client (AndroidDevice::via_server + getprop sys.boot_completed; sync, must run in spawn_blocking); emulator_binary() finds SDK emulator via sdk::find_sdk_root; start(avd) spawns detached (Windows: cmd /c start "" emulator -avd; unix: spawn with null stdio) — same detach intent as start_docker_desktop. 'installed' = sdk::detect().ready(). check-android-sdk route now returns {available,installed,can_start} like check_docker; new POST start-android (defaults to first AVD). /android page gates on reachable: shows Start-emulator prompt + polls, or SDK-not-ready hint. All feature-gated.

NOT done / remaining polish: the /reconciler does NOT auto-start the emulator (Docker has an auto-start path; emulator start is currently only user-triggered via the page button). open_app tool, android-pilot role, long_press still unbuilt. 2FA work remains exploration-only (separate branch). ADR-024 still Proposed. Branch fahreza/android-device-control PUSHED (commits through 2a68f71 are on origin; dd0b77b is local — needs another push). Verified compile both feature states + frontend build; not yet exercised live against a booted emulator.
