Full ADR-017 implementation: publish_app MCP tool in mcp_apps.py, POST /api/apps/publish endpoint that registers app + launches container (auto-detects node/python/alpine from start_command), HTTP proxy at /apps/{rest} that forwards to app containers via Docker inspect. Critical pattern: MutexGuard from db.conn() must be scoped/dropped before any .await or axum rejects the handler as non-Send. Apps MCP server added to default config.
