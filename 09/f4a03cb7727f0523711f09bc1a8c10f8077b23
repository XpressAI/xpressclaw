Standardized the Android SDK preflight as a setup route (was CLI-only doctor). GET /api/setup/check-android-sdk in routes/setup.rs sits beside check_docker/check_ollama, returns {feature_enabled, ready, sdk:<SdkStatus>} from android::sdk::detect() when --features android, else {feature_enabled:false}. Feature plumbing: xpressclaw-server now has its own 'android' feature (=> xpressclaw-core/android); cli 'android' = core+server android. Frontend does NOT call this yet — next step is a setup-wizard SDK card and/or an SDK-status hint in the Android connector add-form. Not pushed.
