feat: streaming task progress. Instead of accumulating the full response and saving once, call_agent now creates a task_message immediately and updates it as content arrives. Updates on </tool_call> boundaries or every 2KB. The 3s frontend poll picks up changes so users see tool calls appear incrementally. Added update_message_content to TaskConversation. Moved message saving from process_response into call_agent (streaming path) and the legacy fallback path. process_response no longer saves to avoid duplicates.
