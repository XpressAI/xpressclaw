android-pilot preset. This CORRECTS the role from the abandoned ae5fbc5 template, whose prompt said 'coordinates come from what you see in the screenshot - never guess' - which is itself the guess that causes the targeting miss. New role teaches the accessibility-first workflow: android_screen_map to perceive (exact coords), android_open_app to launch (not icon-tapping), android_tap_text by label, screenshots only for SEEING visual content not reading coords, re-read the map to confirm each step. Notes FLAG_SECURE (authenticator screenshots are black but the map still reads them) and to save flows to memory. Registered unconditionally in BUILTIN_YAML (not feature-gated) to match the existing pattern - all presets and all connector tiles always show; worst case in a non-android build the agent just lacks android_* tools. recommended_llm: cloud. Preset tests pass, frontend builds.
