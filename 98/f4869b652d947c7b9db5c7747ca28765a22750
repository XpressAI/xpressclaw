feat: ADR-021 agent sessions. Persistent Claude SDK sessions via session_id + continue_conversation. New harness endpoint POST /v1/session/send formats messages as events and runs in persistent context. Processor simplified: routes through harness (has tools), falls back to LLM router (no tools but streaming). Removed all tool description injection, tool call detection, and harness follow-up hack. SDK supports session management: list_sessions, get_session_info, fork_session, resume. Daily lifecycle (new session endpoint) for workspace listing + notes.
