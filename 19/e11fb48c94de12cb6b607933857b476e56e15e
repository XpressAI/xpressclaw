Second site of the PR #117 bug class. #117 stripped the Windows verbatim \?\ prefix only at the bind-mount source in workers/native.rs; Eduardo's 49515ff added the folder picker two days later with its own canonicalize() and no strip, so GET /api/setup/directories returned \?\C:\... as path/parent/roots/entries and DirectoryPicker.svelte:42 stored that form as the agent workspace. Reproduced live against the running server before touching code (home was the only clean field because it comes from USERPROFILE, never through canonicalize()).

Moved the helper to xpressclaw_core::paths instead of copying it a third time; native.rs now imports it. Verified only one caller of canonical_or_original (native.rs:1021) and grepped for workspace string equality across crates/ — none, so configs already holding \?\ paths keep working via the mount-time strip and need no migration.

Known gap in this commit, fixed in the follow-up: strip happens before the fs calls (read_dir, join().is_file()), and \?\ is what lifts MAX_PATH, so deep paths could regress. Also #[cfg(all(test, windows))] means the unit tests never compile on Linux CI — the same blind spot that let #117 ship incomplete. Not yet verified end-to-end; the release build was skipped.
